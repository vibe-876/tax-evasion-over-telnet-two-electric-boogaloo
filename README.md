To build, run `make`, then run the binary.
A server is now running on your computer on port 8080, and run `telnet 127.0.0.1:8080`.

Please note that this game is non-deterministic if ran on eduroam (and possibly other networks too...).
